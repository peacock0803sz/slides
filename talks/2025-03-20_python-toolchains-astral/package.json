{
  "name": "python-toolchains-astral",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev",
    "export:pdf": "mkdir -p dist && slidev export --format pdf --output dist/python-toolchains-astral.pdf",
    "build:aws": "slidev build --base /python-toolchains-astral/",
    "render:cover": "slidev export slides.md --format png --range 1 --output dist/_img/ && mv dist/_img/1.png dist/cover.png && rmdir dist/_img"
  },
  "dependencies": {
    "@peacock0803sz/theme-g-gen": "workspace:*",
    "@slidev/cli": "^51.1.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.50.0"
  }
}
